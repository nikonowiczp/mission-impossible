#ifndef GAMEMODE_H
#define GAMEMODE_H

///
/// \brief The GameMode enum.
///
/// The GameMode enum represents the game mode in the UI classes.
enum class GameMode {
    EASY = 0,
    MEDIUM = 1,
    HARD = 2
};

#endif // GAMEMODE_H
